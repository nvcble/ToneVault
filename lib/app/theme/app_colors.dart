import 'package:flutter/material.dart';

/// Palette seed for the app.
///
/// Amber reads as a warm, gear-adjacent accent and keeps enough contrast
/// against the dark surfaces the app defaults to.
abstract final class AppColors {
  static const Color seed = Color(0xFFFFA000);
}
